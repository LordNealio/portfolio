node_modules
dist
.vercel
.DS_Store
*.local

.vercel

# Secrets — never commit real env files (.env.example is the tracked template)
.env
.env.*
!.env.example

# Book build projects (large binaries; not part of the site)
books/
